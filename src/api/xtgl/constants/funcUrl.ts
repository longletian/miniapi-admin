const BASE = {
  ADD: '_add',
  DELETE: '_delete',
  EDIT: '_edit',
  INFO: '_info',
  LIST: '_list',
};

const COMMON = {
  LOGIN: '/user/login',
  VERIFICATIONCODE: '',
};

const USER = {
  ROOT: '/user',
};

export { USER, COMMON, BASE };
